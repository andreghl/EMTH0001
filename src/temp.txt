import matplotlib.pyplot as plt
import gymnasium as gym
import numpy as np
import env

from tqdm import tqdm

env = gym.make('Responder-v0'); env.reset()

from gymnasium.utils.env_checker import check_env
try:
    check_env(env.unwrapped)
    print("Environment passes all checks!")
except Exception as e:
    print(f"Environment has issues: {e}")

class Agent():

    def __init__(self, env, precision = 10):
        self.env = env
        self.precision = precision

        self.nA = env.action_space.n
        # useful for many RL algorithms
        self.Q = np.zeros((self.precision, self.nA), dtype = np.float64)
        self.N = np.zeros((self.precision, self.nA), dtype = np.int64)

    def reset(self, precision : int | None = None):

        if precision:
            self.precision = precision

        self.Q = np.zeros((self.precision, self.nA), dtype = np.float64)
        self.N = np.zeros((self.precision, self.nA), dtype = np.int64)

    def select_action(self, obs, eps : float = 0.1):

        if np.random.uniform() < eps:
            action = self.env.action_space.sample()
        else:
            state = self.get_state_index(obs)
            action = np.argmax(self.Q[state])
        return action
    
    def get_state_index(self, obs):

        _low = self.env.observation_space.low
        _high = self.env.observation_space.high
        _range = _high - _low
        """
        The index of a state (obs) is determined by normalizating the range of 
        possible offers and the current offer between 0 and 1 and return the integer 
        that corresponds to position of the current offer in the Q table.
        """
        index = ((obs - _low) / _range) * self.precision

        return min(int(index[0]), self.precision - 1)
    
    def update(self, obs, action, reward, next_obs):

        state = self.get_state_index(obs)
        next_state = self.get_state_index(next_obs)

        self.N[state, action] += 1 
        self.Q[state, action] += (reward - self.Q[state, action]) / self.N[state, action]
        
    def eps_greedy(self, eps_min : float = 0.01, n_episodes : int = 1000):

        self.iter = np.zeros(n_episodes,  dtype = np.int64)
        self.actions = np.zeros((n_episodes, self.nA), dtype = np.int64)
        self.p = np.zeros(n_episodes, dtype = np.float64)

        self.nA = env.action_space.n
        self.Q = np.zeros((self.precision, self.nA), dtype = np.float64)
        self.N = np.zeros((self.precision, self.nA), dtype = np.int64)

        print(f'> Running EpsGreedy (min = {eps_min}): \n')

        Qe      = np.empty((n_episodes, 
                            self.precision, self.nA), 
                            dtype = np.float64)
        returns = np.empty(n_episodes, dtype = np.float64)
        actions = np.empty(n_episodes, dtype = np.int64)

        for e in tqdm(range(n_episodes)):
            obs, _ = self.env.reset()
            done = False
            episode_return = 0.0
            last_action = 0

            eps = max(eps_min, 1.0 - e / (n_episodes * 0.8))

            while not done:

                action = self.select_action(obs, eps)
                next_obs, reward, terminated, truncated, info = self.env.step(action)
                done = terminated or truncated

                self.iter[e] += 1
                self.actions[e, action] += 1 


                self.update(obs, action, reward, next_obs)

                episode_return += reward
                last_action = action
                obs = next_obs     

            Qe[e] = self.Q
            returns[e] = episode_return
            actions[e] = last_action
            self.p[e] = self.actions[e, 0] / np.sum(self.actions[e, :])

        return returns, Qe, actions
    


agent = Agent(env, precision = 100)
n_episodes = 100
returns, Qe, actions = agent.eps_greedy(n_episodes = n_episodes)

def ma(x, size = 1):
    w = np.ones(size) / size
    return np.convolve(x, w, mode = 'valid')

size = int(n_episodes / 10)
plt.plot(ma(agent.actions[:, 0], size), c = 'red', label = 'reject')
plt.plot(ma(returns, size), label = "returns")
plt.legend(loc = 'best')
plt.show()